# Simple Audio For Audio Visualizers (safav)

Simple way to handle audio for audio visualizers

**only listens to audio, does not do any playback of audio**

# Features

- Very Fast
- Loopback (Windows / Linux, Mac might not be as simple, also I don't own one)
- minimal dependencies / lightweight


# Example

```rust
// Create host
let mut host = Host::new().unwrap();

// Get list of devices provided by host
let devices = host.devices();

// Creates a new listener that can be shared between threads since host itself can't be shared
let listener = host.create_listener();

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  // Polls data from listener
  let data = listener.poll();
  
  // ...
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data = polling
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data = polling.
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data = polling
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data = pollin
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data = polli
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data = poll
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data = pol
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data = po
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data = p
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data = 
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data = o
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data = 
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data =
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data 
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let data
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let dat
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let da
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let d
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let 
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let _data
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let _dat
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let _da
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let _d
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let _
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let 
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  let
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  le
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  l
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
  
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
 
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {

}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {
}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {}

// Starts the listener to listen to audio
host.listen().unwrap();

loop {

// Starts the listener to listen to audio
host.listen().unwrap();

loop 

// Starts the listener to listen to audio
host.listen().unwrap();

loop

// Starts the listener to listen to audio
host.listen().unwrap();

loo

// Starts the listener to listen to audio
host.listen().unwrap();

lo

// Starts the listener to listen to audio
host.listen().unwrap();

l

// Starts the listener to listen to audio
host.listen().unwrap();



// Starts the listener to listen to audio
host.listen().unwrap();


// Starts the listener to listen to audio
host.listen().unwrap();

// Listen for audio
host.listen().unwrap();

// Listen for audio 
host.listen().unwrap();

// Listen for audio
host.listen().unwrap();

// Listen for audi
host.listen().unwrap();

// Listen for aud
host.listen().unwrap();

// Listen for au
host.listen().unwrap();

// Listen for a
host.listen().unwrap();

// Listen for 
host.listen().unwrap();

// Listen for
host.listen().unwrap();

// Listen fo
host.listen().unwrap();

// Listen f
host.listen().unwrap();

// Listen 
host.listen().unwrap();

// Listen
host.listen().unwrap();

// Liste
host.listen().unwrap();

// List
host.listen().unwrap();

// Lis
host.listen().unwrap();

// Li
host.listen().unwrap();

// L
host.listen().unwrap();

// 
host.listen().unwrap();

//
host.listen().unwrap();

/
host.listen().unwrap();


host.listen().unwrap();

host.listen().unwrap();

host.listen().unwrap()

host.listen().unwrap(

host.listen().unwrap

host.listen().unwra

host.listen().unwr

host.listen().unw

host.listen().un

host.listen().u

host.listen().

host.listen().unwarp

host.listen().unwar

host.listen().unwa

host.listen().unw

host.listen().un

host.listen().u

host.listen().

host.listen()

host.listen()?

host.listen()

host.listen(

host.listen

host.liste

host.list

host.lis

host.li

host.l

host.

host

hos

ho

h




// Get list of devices provided by host
let devices = host.devices();



// Get list of devices provided by host
let devices = host.devices();


// Get list of devices provided by host
let devices = host.devices();

// Get list of devices provided by host,
let devices = host.devices();

// Get list of devices provided by host
let devices = host.devices();

// Get list of devices provided by hos
let devices = host.devices();

// Get list of devices provided by ho
let devices = host.devices();

// Get list of devices provided by h
let devices = host.devices();

// Get list of devices provided by 
let devices = host.devices();

// Get list of devices provided by
let devices = host.devices();

// Get list of devices provided byt
let devices = host.devices();

// Get list of devices provided byt 
let devices = host.devices();

// Get list of devices provided byt
let devices = host.devices();

// Get list of devices provided by
let devices = host.devices();

// Get list of devices provided b
let devices = host.devices();

// Get list of devices provided 
let devices = host.devices();

// Get list of devices provided
let devices = host.devices();

// Get list of devices provide
let devices = host.devices();

// Get list of devices provid
let devices = host.devices();

// Get list of devices provi
let devices = host.devices();

// Get list of devices prov
let devices = host.devices();

// Get list of devices pro
let devices = host.devices();

// Get list of devices pr
let devices = host.devices();

// Get list of devices p
let devices = host.devices();

// Get list of devices 
let devices = host.devices();

// Get list of devices
let devices = host.devices();

// Get list of device
let devices = host.devices();

// Get list of devic
let devices = host.devices();

// Get list of devi
let devices = host.devices();

// Get list of dev
let devices = host.devices();

// Get list of de
let devices = host.devices();

// Get list of d
let devices = host.devices();

// Get list of 
let devices = host.devices();

// Get list of
let devices = host.devices();

// Get list o
let devices = host.devices();

// Get list 
let devices = host.devices();

// Get list
let devices = host.devices();

// Get lis
let devices = host.devices();

// Get li
let devices = host.devices();

// Get l
let devices = host.devices();

// Get 
let devices = host.devices();

// Get
let devices = host.devices();

// Ge
let devices = host.devices();

// G
let devices = host.devices();

// 
let devices = host.devices();

//
let devices = host.devices();

/
let devices = host.devices();


let devices = host.devices();

let devices = host.devices();

let devices = host.devices()

let devices = host.devices(

let devices = host.devices

let devices = host.device

let devices = host.devic

let devices = host.devi

let devices = host.dev

let devices = host.de

let devices = host.d

let devices = host.

let devices = host

let devices = hos

let devices = ho

let devices = h

let devices = 

let devices =

let devices 

let devices

let device

let devic

let devi

let dev

let de

let d

let 

let devicea 

let devicea

let device

let devic

let devi

let dev

let de

let d

let 

let

le

l



```